pay_hours = float(input('Input Hours: '))
pay_rate = float(input('Input Pay Rate: '))

if pay_hours > 40:
    pay = (pay_hours - 40) * (pay_rate * 1.5)
    pay = pay + (40 * pay_rate)
else:
    pay = pay_hours * pay_rate

print('Your Earnings: ${:.2f}'.format(pay))
